---
name: scheduling-optimization-visualization
description: >
  将调度优化结果绘制为适合论文、报告或技术展示的时序图。
  根据实际数据语义自动决定变量分组、正负方向、坐标轴和图形形式，不依赖固定字段名。
---

# Scheduling Optimization Visualization

## Goal

对调度优化结果进行清晰、论文级的可视化。优先突出：

- 各资源在不同时间的出力构成；
- 供需/功率平衡；
- 储能充放电行为；
- 负荷、计划值或其他关键参考曲线；
- 必要的经济、环境或运行指标。

参考风格通常为：

**堆叠柱状图 + 折线 + 必要时双纵轴**

但不要机械模仿示例，应根据当前数据选择最合理的画法。

## Core Principles

### 1. 先理解变量，再画图

根据列名、单位、模型说明和数据特征判断变量属于：

- 功率/能量分量；
- 负荷或参考曲线；
- 状态量，如 SOC；
- 价格、排放、风险等指标；
- 全局 KPI。

不要硬编码 `G1`、`G2`、`PV`、`ESS` 等字段名。

### 2. 可加和的功率优先堆叠

同单位、同时间尺度、物理上可加和的变量可画为堆叠柱状图，例如发电、可再生能源、购电、储能充放电等。

典型展示：

- 供给侧：零轴以上；
- 充电、售电或其他消耗侧：根据模型符号约定放在零轴以下；
- 正值和负值分别累计。

若数据本身已有明确正负号，优先尊重原始定义。

### 3. 关键趋势用折线

负荷、净负荷、调度指令、参考功率、边界等通常用折线叠加。

如果折线与柱状图单位相同，使用同一纵轴。

若是电价、排放强度、LEOI 等不同量纲指标，可使用右纵轴。

双纵轴只在确实有解释价值时使用，不创建第三纵轴。

### 4. 状态量通常单独画

SOC、库存、储氢量、储热量、水位等不参与功率堆叠。

优先作为独立子图，与主功率图共享时间轴。

### 5. 多能源系统分开表达

电、热、冷、气等不同能量载体通常使用不同子图。

不要因为它们都是数值变量就直接堆叠到一起。

## Adaptive Plotting

根据当前数据自动选择：

- **功率平衡明显** → 堆叠柱 + 负荷/目标折线；
- **单个或少量时序变量** → 普通折线/柱状图；
- **SOC 等状态量** → 独立子图；
- **多方案/多算法比较** → 多折线、分组柱、箱线图或其他更合适的比较图；
- **大量时段** → 折线、面积图、热力图或代表性时间窗口，而不是密集柱状图。

Agent 应优先保证图的解释性，而不是套固定模板。

## Visual Style

默认采用简洁论文风格：

- 白色背景；
- 坐标轴带变量名和单位；
- 图例简洁，尽量不遮挡数据；
- 同一设备在不同图中保持相同颜色；
- 可再生能源、储能、常规机组等可按语义选择易区分配色；
- 零轴清晰；
- 时间刻度避免过密；
- 不使用 3D、渐变、重阴影等装饰效果；
- 输出时避免标签或图例被裁切。

对于较小的论文插图，应特别保证缩放后的字体和线条仍可辨认。

## Basic Validation

绘图前做必要检查即可：

- 时间是否正确排序；
- 是否存在 NaN / Inf；
- 堆叠变量单位是否一致；
- 正负号是否符合物理含义；
- 若存在明确功率平衡关系，检查平衡残差；
- SOC 等状态量是否明显越界。

如果发现明显异常，应提示，而不是用图形掩盖问题。

## Recommended Workflow

1. 读取数据和必要的模型/变量说明；
2. 判断每个变量的物理角色；
3. 自动组织功率、折线、状态量和指标；
4. 选择最合适的图形结构；
5. 做基础数据校验；
6. 绘图并输出高质量图片。

建议输出 PNG，同时在适合论文时提供 SVG 或 PDF。

## Rule of Thumb

**能表达“构成”的数据用堆叠，能表达“趋势”的数据用折线，状态量分开画；先尊重物理语义和单位，再考虑视觉效果。**